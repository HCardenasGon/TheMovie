@model ML.Usuario
@{
    ViewData["Title"] = "NewPassword";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CodePen - Log In / Sign Up - pure css - #12</title>
    <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/css/bootstrap.min.css'>
    <link rel='stylesheet' href='https://unicons.iconscout.com/release/v2.1.9/css/unicons.css'>
    <link rel="stylesheet" href="~/css/Style.css">
</head>
<body>
    <!-- partial:index.partial.html -->
    <a href="https://front.codes/" class="logo" target="_blank">
        <img src="https://assets.codepen.io/1462889/fcy.png" alt="">
    </a>

    <div class="section">
        <div class="container">
            <div class="row full-height justify-content-center">
                <div class="col-12 text-center align-self-center py-5">
                    <div class="section pb-5 pt-5 pt-sm-2 text-center">
                        <div class="card-3d-wrap mx-auto">
                            <div class="card-3d-wrapper">
                                <div class="card-front">
                                    <div class="center-wrap">
                                        <div class="section text-center">
                                            @using (Html.BeginForm("NewPassword", "Usuario", FormMethod.Post, new { enctype = "multipart/form-data" }))
                                            {
                                                <div class="col">
                                                    @Html.TextBoxFor(model => model.Email, new {@type = "hidden"})
                                                </div>

                                                <h4 class="mb-4 pb-3">New Password</h4>
                                                <div class="form-group mt-2">
                                                    <input type="password" name="password" class="form-style" placeholder="New Password" id="password" autocomplete="off">
                                                    <i class="input-icon uil uil-lock-alt"></i>
                                                </div>
                                                <div class="form-group mt-2">
                                                    <input type="password" name="confirmPassword" class="form-style" placeholder="Repeat Password" id="confirmPassword" autocomplete="off">
                                                    <i class="input-icon uil uil-shield"></i>
                                                </div>
                                                <div class="form-group mt-2">
                                                    <span id='message'></span>
                                                </div>
                                                <button type="submit" href="#" class="btn mt-4" id="btnSubmit">submit</button>
                                            }
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- partial -->
    <script src="./script.js"></script>
</body>
</html>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

<script type="text/javascript">
    $('#password, #confirmPassword').on('keyup', function () {
        if ($('#password').val() == $('#confirmPassword').val()) {
            $('#message').html('Password match').css('color', 'green');
            $('#btnSubmit').attr("disabled", false);
        } else{
            $('#message').html("Password doesn't match").css('color', 'red');
            $('#btnSubmit').attr('disabled', 'disabled');
        }
    });
</script>